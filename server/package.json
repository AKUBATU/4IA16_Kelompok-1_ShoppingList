{
  "name": "server",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "prisma db push && prisma generate && nodemon src/index.js",
    "start": "prisma db push && prisma generate && node src/index.js",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8",
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "prisma": "^5.20.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}